# Audio placeholder

Canvas 테스트 장면의 임시 사운드트랙을 아래 이름으로 넣습니다.

```text
test-scene-soundtrack.mp3
```

경로는 `app/config/playerConfig.ts`의 `animationAudioSource`에서 변경할 수 있습니다. 파일이 없어도 Canvas 영상은 정상 재생됩니다.
